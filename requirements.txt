# Python依赖配置 - 统一管理所有Python依赖
# 安装命令: cd llm && source venv/bin/activate && pip install -r ../requirements.txt

# ============================================
# LLM模块依赖
# ============================================
python-dotenv>=1.0.0
loguru>=0.7.0
litellm>=1.0.0

# ============================================
# 后端服务依赖（COS上传）
# ============================================
cos-python-sdk-v5>=1.9.24

